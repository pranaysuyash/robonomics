{
  "defaultProviderOrder": ["manual", "hermes"],
  "providers": [
    {
      "id": "hermes",
      "type": "command",
      "enabled": true,
      "command": "hermes",
      "args": ["chat", "-Q", "--provider", "openai-codex", "-m", "gpt-5", "-q", "{{PAYLOAD}}"],
      "inputMode": "none",
      "timeoutMs": 300000,
      "instruction": "Use your available web/search/browser tools, verify sources, and return valid JSON only matching the contract."
    },
    {
      "id": "manual",
      "type": "manual",
      "enabled": true
    }
  ]
}
