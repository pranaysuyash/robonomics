{
  "defaultProviderOrder": ["gemini", "hermes", "manual"],
  "providers": [
    {
      "id": "gemini",
      "type": "gemini",
      "enabled": true,
      "model": "gemini-3.1-pro-preview",
      "instruction": "Perform web research and return JSON findings only."
    },
    {
      "id": "hermes",
      "type": "command",
      "enabled": true,
      "command": "hermes",
      "args": ["chat", "-Q", "--provider", "openai-codex", "-m", "gpt-5", "-q", "{{PAYLOAD}}"],
      "inputMode": "none",
      "timeoutMs": 300000,
      "instruction": "Use your available web/search/browser tools, verify sources, and return valid JSON only matching the schema."
    },
    {
      "id": "manual",
      "type": "manual",
      "enabled": true
    }
  ]
}
